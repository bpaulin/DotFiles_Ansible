---
- hosts: localhost
  vars_files:
    - vars/all.yml
    - vars/user.yml
  vars:
    system:
      user: bpaulin
  roles:
   - system
   - git
   - terminal
   - xml
   - php
   - nodejs
   - ruby
   - { role: atom, when: with_gui == True }
   - { role: chrome, when: with_gui == True }
